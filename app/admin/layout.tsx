
import '@styles/admin.css';
import { poppins, ptserif } from '@styles/fonts';
import Link from 'next/link';

export default function RootLayout({ children }: { children: JSX.Element }) {

    return (
        <div className='sticky'>
            home
        </div>
    )
}
